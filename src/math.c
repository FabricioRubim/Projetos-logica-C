#include <stdio.h>

int main()
{
    // declaração da variavel de um tipo inteiro
    int n = 0;

    // Looping em do while para fazer essa função e ver se ela é verdadeira
    do
    {
        n++;
        printf("%d\n", n);
    } while (n < 5);
    
    return 0;
}